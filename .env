# This should be ignored because .mcp.json has "TEST_KEY"
TEST_KEY=env_value
# This should be picked up as ".env" source
OTHER_KEY=env_key
# This should be picked up as "process.env" source
PROCESS_KEY=process_val
